/**
 * @file Constants of app
 * @module constants/app
 * @author ntnyq <https://github.com/ntnyq>
 */

export enum Theme {
  DEFAULT = `theme-default`,
}

export enum UserRole {
  ADMIN = `admin`,
  USER = `user`,
}
