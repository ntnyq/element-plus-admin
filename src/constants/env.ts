/**
 * @file Env Constants
 * @module constants/env
 * @description Constants for the env.
 */

export const appTitle = import.meta.env.VITE_APP_TITLE

export const ENV = Object.freeze({
  appTitle,
})
